# LoanStatusPrediction
Here’s a slightly shorter version under 300 characters:  ---  Predict loan default using machine learning with financial and demographic data. Uses logistic regression, handles class imbalance with SMOTE, and includes preprocessing, model training, evaluation (accuracy, recall, F1, ROC AUC), and visualizations for reliable decisions.
